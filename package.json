{
  "name": "integrity-dash",
  "private": true,
  "version": "0.5.1-alpha-demo",
  "type": "module",
  "description": "Integrity Dash - a browser-based 3D endless runner. Run Fast. Choose Right.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "format": "prettier --write .",
    "validate": "tsc -p tsconfig.validate.json && node scripts/validate.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@types/three": "^0.169.0",
    "eslint": "^9.12.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.8.1",
    "vite": "^5.4.9"
  }
}
